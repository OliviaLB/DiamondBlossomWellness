# The live site's origin - no trailing slash, e.g. https://www.example.co.uk
# Drives canonical URLs, social-share image URLs, robots.txt and sitemap.xml.
# Left blank, the build still works but omits those (and warns) rather than guess.
VITE_APP_URL=
